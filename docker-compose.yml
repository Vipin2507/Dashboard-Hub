services:
  api:
    build:
      context: .
      dockerfile: server/Dockerfile
    container_name: buildesk-api
    ports:
      - "4000:4000"
    environment:
      - PORT=4000
      - SQLITE_PATH=/app/data/app.db
    volumes:
      - ./data:/app/data
    restart: unless-stopped
